(function () {
  'use strict';

  // Service worker kept minimal — content scripts listen to
  // chrome.storage.onChanged directly for settings updates.
  // This file exists to satisfy the manifest's service_worker entry.
})();
